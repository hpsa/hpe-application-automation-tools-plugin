<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <artifactId>teamcity-plugin-root</artifactId>
        <groupId>com.hp.mqm</groupId>
        <version>12.53.19-SNAPSHOT</version>
    </parent>

	<artifactId>build</artifactId>
	<packaging>pom</packaging>
	<properties>
		<maven.build.timestamp.format>yyyyMMddHHmmss</maven.build.timestamp.format>
		<teamcity.plugin.version>${version}</teamcity.plugin.version>
		<teamcity.plugin.vendorName>Plugin vendor name</teamcity.plugin.vendorName>
		<teamcity.plugin.vendorUrl>Plugin vendor URL</teamcity.plugin.vendorUrl>
		<zip.folder>../target</zip.folder>
		<zip.name>hp-lifecycle-management-teamcity-ci-plugin</zip.name>
		<zip.full.name>${zip.folder}/${zip.name}.zip</zip.full.name>
		<zip.location>${zip.folder}/${zip.name}.zip</zip.location>
		<extraction.folder>${project.build.directory}/extracted</extraction.folder>
		<temp.target>${project.build.directory}/temp</temp.target>
		<!--jar.folder>${extraction.folder}/server/</jar.folder-->
		<jar.folder>server</jar.folder>


	</properties>
	<dependencies>
		<dependency>
			<groupId>com.hp.mqm</groupId>
			<artifactId>hp-lifecycle-management-teamcity-ci-plugin</artifactId>
			<version>${project.version}</version>
		</dependency>
	</dependencies>
	<build>
		<plugins>
			<plugin>
				<groupId>com.google.code.maven-replacer-plugin</groupId>
				<artifactId>replacer</artifactId>
				<version>1.5.2</version>
				<executions>
					<execution>
						<phase>process-sources</phase>
						<goals>
							<goal>replace</goal>
						</goals>
					</execution>
				</executions>
				<configuration>
					<file>${basedir}/../teamcity-plugin.xml</file>
					<outputFile>${basedir}/target/teamcity-plugin.xml</outputFile>
					<replacements>
						<replacement>
							<token>@Version@</token>
							<value>${teamcity.plugin.version}</value>
						</replacement>
						<replacement>
							<token>@VendorName@</token>
							<value>${teamcity.plugin.vendorName}</value>
						</replacement>
						<replacement>
							<token>@VendorURL@</token>
							<value>${teamcity.plugin.vendorUrl}</value>
						</replacement>
					</replacements>
				</configuration>
			</plugin>
			<plugin>
				<artifactId>maven-assembly-plugin</artifactId>
				<version>2.4</version>
				<executions>
					<execution>
						<id>make-assembly</id>
						<phase>package</phase>
						<goals>
							<goal>single</goal>
						</goals>
						<configuration>
							<finalName>hp-lifecycle-management-teamcity-ci-plugin</finalName>
							<outputDirectory>${project.parent.build.directory}</outputDirectory>
							<appendAssemblyId>false</appendAssemblyId>
							<descriptors>
								<descriptor>plugin-assembly.xml</descriptor>
							</descriptors>
						</configuration>
					</execution>
				</executions>
			</plugin>
		</plugins>
	</build>

	<profiles>
		<profile>
			<id>deployPluginDevEnv</id>
			<activation>
				<property>
					<name>deployTeamCityPlugin</name>
					<value>true</value>
				</property>
			</activation>
			<build>
				<plugins>
					<plugin>
						<artifactId>exec-maven-plugin</artifactId>
						<groupId>org.codehaus.mojo</groupId>
						<executions>
							<execution><!-- Run our version calculation script -->
								<phase>package</phase>
								<goals>
									<goal>exec</goal>
								</goals>
								<configuration>
									<executable>${basedir}/../misc/scripts/deployPlugin.bat</executable>
								</configuration>
							</execution>
						</executions>
					</plugin>
				</plugins>
			</build>
		</profile>
		<profile>
			<id>jar-signing</id>
			<build>
				<defaultGoal>install</defaultGoal>
				<plugins>
					<plugin>
						<artifactId>maven-antrun-plugin</artifactId>
						<version>1.8</version>
						<executions>
							<execution>
								<id>unzip</id>
								<phase>pre-integration-test</phase>
								<configuration>
									<tasks>
										<echo message="unziping file ${zip.full.name}" />
										<unzip src="${zip.full.name}" dest="${extraction.folder}" />
									</tasks>
								</configuration>
								<goals>
									<goal>run</goal>
								</goals>
							</execution>
							<execution>
								<id>zip</id>
								<phase>post-integration-test</phase>
								<configuration>
									<tasks>
										<copy file="${extraction.folder}/teamcity-plugin.xml" todir="${project.build.directory}/${zip.name}"/>
										<echo message="ziping jars into ${zip.full.name}" />
										<zip destfile="${zip.full.name}" basedir="${project.build.directory}/${zip.name}"/>
									</tasks>
								</configuration>
								<goals>
									<goal>run</goal>
								</goals>
							</execution>
						</executions>
					</plugin>
					<plugin>
						<groupId>SignHPUtility.plugin</groupId>
						<artifactId>signhp-maven-plugin</artifactId>
						<executions>
							<execution>
								<phase>integration-test</phase>
								<goals>
									<goal>signHP</goal>
								</goals>
								<configuration>
									<parameters>-s ${extraction.folder}/${jar.folder} -t 24 -p AGN_JAR -i -v -u jarsigner -c 1 -o ${project.build.directory}/${zip.name}/${jar.folder}</parameters>
									<properties>
										<krb.account_name>qcbuilder@EMEA.CPQCORP.NET</krb.account_name>
										<krb.password>red.mud-83</krb.password>
									</properties>
								</configuration>
							</execution>
						</executions>
					</plugin>
				</plugins>
			</build>
		</profile>
	</profiles>
</project>
