<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <artifactId>teamcity-plugin-root</artifactId>
        <groupId>com.hp.mqm</groupId>
        <version>12.53.7-SNAPSHOT</version>
    </parent>

	<artifactId>build</artifactId>
	<packaging>pom</packaging>
	<properties>
		<maven.build.timestamp.format>yyyyMMddHHmmss</maven.build.timestamp.format>
		<teamcity.plugin.version>snapshot-${maven.build.timestamp}</teamcity.plugin.version>
		<teamcity.plugin.vendorName>Plugin vendor name</teamcity.plugin.vendorName>
		<teamcity.plugin.vendorUrl>Plugin vendor URL</teamcity.plugin.vendorUrl>
		<zip.location>${project.parent.basedir}/target/hp-lifecycle-management-teamcity-ci-plugin.zip</zip.location>
		<extraction.folder>${project.build.directory}/extracted</extraction.folder> 
		<temp.target>${project.build.directory}/temp</temp.target>
		<jar.folder>${extraction.folder}/server/</jar.folder> 
		
	</properties>
	<dependencies>
		<dependency>
			<groupId>com.hp.mqm</groupId>
			<artifactId>hp-lifecycle-management-teamcity-ci-plugin</artifactId>
			<version>${project.version}</version>
		</dependency>
	</dependencies>
	<build>
		<plugins>
			<plugin>
				<groupId>com.google.code.maven-replacer-plugin</groupId>
				<artifactId>replacer</artifactId>
				<version>1.5.2</version>
				<executions>
					<execution>
						<phase>process-sources</phase>
						<goals>
							<goal>replace</goal>
						</goals>
					</execution>
				</executions>
				<configuration>
					<file>${basedir}/../teamcity-plugin.xml</file>
					<outputFile>${basedir}/target/teamcity-plugin.xml</outputFile>
					<replacements>
						<replacement>
							<token>@Version@</token>
							<value>${teamcity.plugin.version}</value>
						</replacement>
						<replacement>
							<token>@VendorName@</token>
							<value>${teamcity.plugin.vendorName}</value>
						</replacement>
						<replacement>
							<token>@VendorURL@</token>
							<value>${teamcity.plugin.vendorUrl}</value>
						</replacement>
					</replacements>
				</configuration>
			</plugin>
			<plugin>
				<artifactId>maven-assembly-plugin</artifactId>
				<version>2.4</version>
				<executions>
					<execution>
						<id>make-assembly</id>
						<phase>package</phase>
						<goals>
							<goal>single</goal>
						</goals>
						<configuration>
							<finalName>hp-lifecycle-management-teamcity-ci-plugin</finalName>
							<outputDirectory>${project.parent.build.directory}</outputDirectory>
							<appendAssemblyId>false</appendAssemblyId>
							<descriptors>
								<descriptor>plugin-assembly.xml</descriptor>
							</descriptors>
						</configuration>
					</execution>
				</executions>
			</plugin>
		</plugins>
	</build>

	<profiles>
		<profile>
			<id>deployPluginDevEnv</id>
			<activation>
				<property>
					<name>deployTeamCityPlugin</name>
					<value>true</value>
				</property>
			</activation>
			<build>
				<plugins>
					<plugin>
						<artifactId>exec-maven-plugin</artifactId>
						<groupId>org.codehaus.mojo</groupId>
						<executions>
							<execution><!-- Run our version calculation script -->
								<phase>package</phase>
								<goals>
									<goal>exec</goal>
								</goals>
								<configuration>
									<executable>${basedir}/../misc/scripts/deployPlugin.bat</executable>
								</configuration>
							</execution>
						</executions>
					</plugin>
				</plugins>
			</build>
		</profile>
		<profile>
			<id>jar-signing</id>
			<build>
				<plugins>
					<plugin>
						<groupId>org.codehaus.mojo</groupId>
						<artifactId>exec-maven-plugin</artifactId>
						<executions>
							<execution>
								<id>script-chmod</id>
								<phase>pre-integration-test</phase>
								<goals>
									<goal>exec</goal>
								</goals>
								<configuration>
									<executable>chmod</executable>
									<arguments>
										<argument>+x</argument>
										<argument>${basedir}/../misc/scripts/jarSigning.py</argument>
									</arguments>
								</configuration>
							</execution>
							<execution>
								<id>teamcity jars signing</id>
								<phase>pre-integration-test</phase>
								<goals>
									<goal>exec</goal>
								</goals>
								<configuration>
									<executable>${basedir}/../misc/scripts/jarSigning.py </executable>
									<commandlineArgs>${zip.location} ${extraction.folder} ${temp.target} ${jar.folder} </commandlineArgs>
								</configuration>
							</execution>
						</executions>
					</plugin>
				</plugins>
			</build>
		</profile>
	</profiles>
</project>
